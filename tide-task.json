{
    "name": "ShorelineDetection_beta",
    "version": "0.0.4",
    "description": "Writes tide prediction JSON, shoreline GeoJSON, and NDWI GeoTIFF.",
    "properties": {
        "isPublic": true,
        "timeout": 7200
    },
    "inputPortDescriptors": [
        {
            "name": "lat",
            "type": "string",
            "description": "Latitude of image centroid (tide prediction).",
            "required": true
        },
        {
            "name": "lon",
            "type": "string",
            "description": "Longitude of image centroid (tide prediction).",
            "required": true
        },
        {
            "name": "dtg",
            "type": "string",
            "description": "Date time group (Y-m-d-H-M) of image collection (tide prediction).",
            "required": true
        },
        {
            "name": "meta",
            "type": "string",
            "description": "Acquisition metadata.",
            "required": true
        },
        {
            "name": "image",
            "type": "directory",
            "description": "The multispectral image (NDWI and shoreline detection).",
            "required": true
        }
    ],
    "outputPortDescriptors": [
        {
            "name": "results",
            "type": "directory",
            "description": "Output directory containing results (predicted tides, shoreline vector, and NDWI raster)."
        }
    ],
    "containerDescriptors": [
        {
            "type": "DOCKER",
            "properties": {
                "image": "venicegeo/shoreline-task"
            },
            "command": "python /shoreline-task.py"
        }
    ]
}
